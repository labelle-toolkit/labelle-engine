.{
    .version = 1,
    .name = "example_parent_refs",
    .initial_scene = "main",
    .backend = .raylib,
    .ecs_backend = .zig_ecs,
    .window = .{
        .width = 800,
        .height = 600,
        .title = "Parent References & onReady Example",
        .target_fps = 60,
    },
    .camera = .{
        .x = 400,
        .y = 300,
        .zoom = 1.0,
    },
}
