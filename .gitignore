# Zig build artifacts
.zig-cache/
zig-out/

# Example build artifacts
usage/*/.zig-cache/
usage/*/zig-out/

# Screenshots from CI tests
screenshot_*.png

# Note: build.zig, build.zig.zon, and main.zig can be generated by:
#   zig build generate -- <project_path>
# User projects should add these to their .gitignore if regenerating frequently
