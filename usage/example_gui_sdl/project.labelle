.{
    .version = 1,
    .name = "example_gui_sdl",
    .description = "GUI demo with SDL2 backend",
    .initial_scene = "main",
    .backend = .sdl,
    .window = .{
        .width = 800,
        .height = 600,
        .title = "GUI Example (SDL2)",
        .target_fps = 60,
    },
    .resources = .{},
    .plugins = .{},
}
